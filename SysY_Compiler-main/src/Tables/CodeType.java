package Tables;

public enum CodeType {
    INI, MAI, BKI, DBK,
    LDA, LOD, BRK,
    LDC, JMP, JMC, JMT, JMF, MKS,
    ADD, SUB, MUL, DIV, MOD,
    MUS, NOT, SWP, LAV, LCA, STO, ORR, AND, EQL, NEQ, LSS, LEQ, GRT, GEQ,
    CAL, RET, VRE, EAT,
    WRF, RDI
}
